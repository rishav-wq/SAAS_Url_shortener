PORT=5001 
MONGO_URI=mongodb+srv://rishav:rishav123456@cluster0.xjrprvm.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=xjfkfj12345ff
NODE_ENV=production

# VERCEL DEPLOYMENT - Frontend and backend on same domain
FRONTEND_URL=https://your-app.vercel.app
BASE_URL=https://your-app.vercel.app

# CORS ORIGINS - same domain, no cross-origin issues
CORS_ORIGINS=https://your-app.vercel.app,http://localhost:5173
# BASE_URL=https://links.startup.co

# FOR LOCAL DEVELOPMENT ONLY (uncomment next line):
# BASE_URL=http://localhost:5001

# For a real deployment, you need:
# 1. A domain name (buy from GoDaddy, Namecheap, etc.)
# 2. Point the domain to your server IP
# 3. Set up SSL certificate (Let's Encrypt is free)
# 4. Set NODE_ENV=production

NODE_ENV=development

# Email Configuration (configure for production)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ==== DEPLOYMENT INSTRUCTIONS ====
# 1. Deploy to your hosting platform (Vercel, Netlify, Railway, etc.)
# 2. Set BASE_URL environment variable to your production domain
# 3. Configure custom domain in your hosting platform
# 4. Update this value and your users will get proper short links!